import { HighlightDoneTodoDirective } from './highlight-done-todo.directive';

describe('HighlightDoneTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDoneTodoDirective();
    expect(directive).toBeTruthy();
  });
});
